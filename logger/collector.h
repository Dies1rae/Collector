#pragma once
class collector{
private:
	//need to store data from scan and store to log
	//log class, some map class to store open data like regestry
public:
	//init 
	//work loop with EXIT
	//storing function
	//parsing function
	//binary copy file function
};

